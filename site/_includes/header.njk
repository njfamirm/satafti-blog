<header class="flex flex-none items-center bg-white py-10">
  <div
    class="flex flex-col text-center sm:flex-row sm:items-center sm:justify-between space-y-6 sm:space-y-0 container xl:max-w-7xl mx-auto px-4 lg:px-10">
    <div>
      <a href="/"
        class="inline-flex items-center space-x-2 font-bold text-lg tracking-wide text-yellow-600 hover:text-yellow-400">
        <i>njfamirm</i>
      </a>
    </div>

    <nav class="text-sm md:text-base space-x-4 md:space-x-6">
      {% for menu in nav.main %}
        <a class="font-semibold text-gray-500 hover:text-gray-500
          {% if page.url == menu.link %} text-gray-950 underline {% endif %}" href="{{ menu.link }}">{{menu.label }}</a>
      {% endfor %}
    </nav>
  </div>
</header>
